{
  "version": 2,
  "builds": [
    {
      "src": "sipsnob/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "sipsnob/dist"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "sipsnob/dist/$1" }
  ]
}
